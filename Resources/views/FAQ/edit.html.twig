{% extends 'CMSBundle:Default:manager.html.twig' %}

{% block body -%}
    <h1>{{'Редактировать вопрос'|trans}}</h1>

    {{ form(edit_form) }}

    <div class="row form-group">
        <div class="col-md-10 col-md-push-2">
            <a class="btn btn-default" href="{{ path('manager_faq') }}">
	            {{'Назад'|trans}}
	        </a>
	    </div>
	</div>
    <div class="row form-group">
        <div class="col-md-10 col-md-push-2">
            {{ form(delete_form) }}
        </div>
    </div>
{% endblock %}
