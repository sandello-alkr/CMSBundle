{% extends 'CMSBundle:Default:manager.html.twig' %}

{% block body -%}
	<h1>{{'Новый слайд'|trans}}</h1>

	{{ form(form,{'style':'horizontal'}) }}

	<div class="row form-group">
		<div class="col-md-10 col-md-push-2">
			<a class="btn btn-default" href="{{ path('manager_slide') }}">
				{{'Назад'|trans}}
			</a>
		</div>
	</div>
{% endblock %}
