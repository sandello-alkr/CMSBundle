{% extends "CMSBundle:Templates:"~template|default('one_sidebar.html.twig') %}

{% block body -%}
    <h1>Review edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('review') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
