{% extends "CMSBundle:Templates:"~template|default(cms.base_template) %}

{% block body -%}
    <h1>{{'Редактировать отзыв'|trans}}</h1>

    {{ form(edit_form) }}

    <div class="row form-group">
        <div class="col-md-10 col-md-push-2">
            <a class="btn btn-default" href="{{ path('review') }}">
	            {{'Назад'|trans}}
	        </a>
	    </div>
	</div>
    <div class="row form-group">
        <div class="col-md-10 col-md-push-2">
            {{ form(delete_form) }}
        </div>
    </div>
{% endblock %}
