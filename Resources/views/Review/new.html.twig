{% extends "CMSBundle:Templates:"~template|default(base_template) %}

{% block body -%}
    <h1>{{'Новый отзыв'|trans}}</h1>

    {{ form(form) }}

    <div class="row form-group">
        <div class="col-md-10 col-md-push-2">
            <a class="btn btn-default" href="{{ path('review') }}">
	            {{'Назад'|trans}}
	        </a>
	    </div>
	</div>
{% endblock %}
